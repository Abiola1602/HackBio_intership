HACKBIO2025_TASK0
PROJECT1.sh
#!/bin/bash
 echo 'Abiola'			#Print name

 mkdir Abiola			#making directory 

 cd Abiola/biocomputing/	#making directory without having to change directory

 wget https://raw.githubusercontent.com/josoga2/dataset-repos/main/wildtype.fna	 #downloading from website
 wget https://raw.githubusercontent.com/josoga2/dataset-repos/main/wildtype.gbk
 wget https://raw.githubusercontent.com/josoga2/dataset-repos/main/wildtype.gbk
  
 mv wildtype.fna Abiola/		#moving a file to a directory

 rm wildtype.gbk.1			#removing duplicate


 grep 'tatatata' wildtype.fna 	#.fna is a mutatnt has it contain sequence 'tatatata' 
 grep 'tata' wildtype.fna	#It contain both the normal tata and the tatatata, so its a mutant

 grep 'tatatata' wildtype.fna > mutant.txt  # greping mutated sequence into a new file

 tail -n +2 wildtype.gbk | wc -l		#word count of wildtype.gbk excluding header


 grep 'LOCUS' wildtype.gbk   #counting the sequence length using locus as the reference point

 grep 'SOURCE' wildtype.gbk 
 
 grep '/gene=' wildtype.gbk  #listing genes present in wildtype.gbk

 history > history.txt    #printing all command line
 

Project 2: Installing Bioinformatics Software on the Terminal

wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh	# download conda

bash Miniconda3-latest-Linux-x86_64.sh						# run the installer

#Accept the license. #Choose the install location (default is fine)
#Let it run conda init so Conda is available in new shells.
#Close and reopen terminal, then check conda version

 conda --version					# check conda version to confirm installation

#activate channels for bioinformatics

conda config --add channels defaults
conda config --add channels bioconda			
conda config --add channels conda-forge

 conda create -n funtools python=3.10			#create funtools

 conda activate funtools				#activate funtools
 
 sudo apt update; sudo apt install figlet #install figlet

 figlet "Rukayat Abiola Abdulsalam"		#figlet my name
       
             
#Installing tools using bioconda channel

 conda install -c bioconda bwa	
 bwa --version  			#confirming installation
 conda install -c bioconda blast
 blast --version			#confirming installation
 conda install -c bioconda samtools
 samtools --version			#confirming installation
 conda install -c bioconda bedtools
 bedtools --version 			#confirming installation
 conda install -c bioconda spades 
 spades --version			#confirming installation
 conda install -c bioconda bcftools
 bcftools --version			#confirming installation
 conda install -c bioconda fastp 
 fastp --version			#confirming installation
 conda install -c conda-forge -c bioconda multiqc
 multiqc --version			#confirming installation

#make sure funtools is activated. 
#Note package name is spades while .py is just the executable script inside SPAdes

spades.py --version			#run version to know its installed


        # make sure to always activate funtools

